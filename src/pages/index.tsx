import Layout from "@/common/layout";
import HomePage from "@/components/home";

export const Home = (): JSX.Element => {
  return (
    <>
      <Layout>
        <HomePage />
      </Layout>
    </>
  );
};

export default Home;
